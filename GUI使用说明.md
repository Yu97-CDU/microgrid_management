# 新能源微电网运行与经济性分析平台 GUI 使用说明

## 1. 软件定位

本软件是一个面向新能源微电网拓扑构建、运行分析和经济性评估的桌面 GUI 工具。用户可以通过拖拽元件、连接端口、配置参数和运行分析，快速搭建含光伏、风机、储能、电解槽、燃料电池、电网、负荷和电力电子变换器的微电网系统。

当前软件主要适用于以下场景：

- 微电网方案设计阶段的拓扑表达；
- 新能源发电、储能和电力电子接口关系展示；
- 运行功率、线路损耗、变换器损耗和储能 SOC 的初步分析；
- 日运行成本、LCOE 和可再生能源占比等经济性指标估算；
- 教学、论文示意图和方案论证中的交互式建模。

需要注意：当前版本采用简化潮流和启发式调度模型，适合方案级分析和可视化验证，不等同于工业级精确潮流、保护校核或电磁暂态仿真。

---

## 2. 运行环境与启动方式

### 2.1 推荐环境

项目当前使用环境：

```text
D:\anaconda3\envs\science
```

主要依赖包括：

- Python 3.10+
- PyQt6
- numpy
- networkx
- matplotlib

### 2.2 启动方式

在项目目录下运行：

```powershell
D:\anaconda3\envs\science\python.exe E:\Python_projects\pytools\gridmanagement\main.py
```

也可以双击项目中的：

```text
run_gui.bat
```

启动后会进入主界面。

---

## 3. 主界面结构

软件界面由三部分组成：

### 3.1 左侧：元件库

左侧元件库按照功能分类展示微电网元件：

- 分布式电源：光伏、风机、微燃机、燃料电池；
- 储能系统：电池储能、超级电容、飞轮储能、超导储能；
- 变换设备：DC/AC、AC/DC、普通 DC/DC、双向 DC/DC、PCS；
- 电网与负荷：大电网、交流母线、直流母线、负荷、电解槽。

元件名称后带有英文缩写，例如：

- 光伏：PV；
- 风机：WT；
- 电池储能：BESS；
- 逆变器：DC/AC；
- 整流器：AC/DC；
- 双向 DC/DC：Bi-DC/DC；
- 储能变流器：PCS。

### 3.2 中央：拓扑画布

中央画布用于绘制微电网拓扑。画布支持：

- 拖拽放置元件；
- 拖动元件调整位置；
- 从端口拉线连接元件；
- 鼠标滚轮缩放；
- 鼠标右键或中键平移；
- Delete 或 Backspace 删除选中元件或线路；
- 加载示例后自动缩放到完整拓扑范围。

### 3.3 右侧：运行与经济性分析

右侧面板包含两个页签：

- 分析结果：显示节点数量、线路数量、发电功率、负荷功率、线路损耗、变换器损耗、运行成本、投资估算和 LCOE；
- 运行曲线：显示 24 小时功率平衡、储能动作、线路损耗、变换器损耗、运行成本和储能 SOC 曲线。

---

## 4. 基本操作流程

### 4.1 新建微电网拓扑

1. 从左侧元件库拖拽元件到中央画布。
2. 调整各元件位置。
3. 从元件端口的小圆点处按下鼠标左键，拖动到另一个元件端口后释放。
4. 连接成功后，画布上会生成直角折线。
5. 点击工具栏中的“运行分析”。
6. 查看右侧分析结果和运行曲线。

### 4.2 加载示例系统

工具栏提供两个示例按钮：

- 加载示例系统；
- 加载综合示例。

#### 加载示例系统

该示例展示基础新能源微电网结构，包括：

- 大电网；
- 交流母线；
- 直流母线；
- 光伏；
- 风机；
- 电池储能；
- 电解槽；
- DC/DC；
- 双向 DC/DC；
- AC/DC；
- DC/AC。

其典型结构为：

```text
PV -> DC/DC -> DC Bus -> DC/AC -> AC Bus
WT -> AC/DC -> DC Bus -> DC/AC -> AC Bus
Battery -> Bi-DC/DC -> DC Bus
DC Bus -> EL
Grid -> AC Bus -> Load
```

#### 加载综合示例

综合示例进一步加入：

- 燃料电池；
- 超级电容；
- 飞轮储能；
- PCS 储能变流器；
- 多个储能和电力电子接口。

该示例更适合展示多能源耦合微电网。

---

## 5. 元件连接规则与拓扑提示

软件内置了拓扑合理性检查。若用户连接方式不符合常见工程逻辑，界面会在状态栏和右侧提示区给出建议。

### 5.1 光伏接入

光伏输出为直流电，因此不建议直接接入交流母线。

推荐接入路径：

```text
PV -> DC/DC -> DC Bus -> DC/AC -> AC Bus
```

其中：

- DC/DC 用于 MPPT 和直流电压匹配；
- DC/AC 用于将直流母线能量并入交流侧。

### 5.2 风机接入

风机输出通常随风速变化，频率和幅值不稳定，不宜直接接入工频交流母线。

推荐接入路径：

```text
WT -> AC/DC -> DC Bus -> DC/AC -> AC Bus
```

其中：

- AC/DC 将变频交流整流到直流侧；
- DC/AC 再将直流侧能量转换为工频交流。

### 5.3 储能接入

电池、超级电容、飞轮储能、超导储能等在当前软件中默认视为储能本体。

电池和超级电容等直流储能推荐接入路径：

```text
Battery -> Bi-DC/DC -> DC Bus
SC -> Bi-DC/DC -> DC Bus
```

若需要接入交流母线，可以通过 PCS：

```text
Storage -> PCS -> AC Bus
```

说明：

- 双向 DC/DC 用于充放电功率控制、电压匹配和直流母线接入；
- PCS 用于储能系统与交流母线或电网之间的双向功率交换。

### 5.4 电解槽接入

电解槽通常需要直流供电，推荐接入直流母线：

```text
DC Bus -> EL
```

若从交流侧供电，建议增加 AC/DC 整流器。

### 5.5 端口类型

元件端口带有类型：

- AC：工频交流端口；
- DC：直流端口；
- AC_VAR：风机等变频交流端口；
- ANY：通用端口。

端口类型不匹配时，系统会给出拓扑提示。

---

## 6. 参数配置

### 6.1 配置元件参数

双击画布中的元件，会弹出参数配置窗口。不同元件具有不同参数。

光伏示例参数：

- 额定功率；
- 当前出力系数；
- 光照强度；
- 工作温度；
- 接入电压等级。

电池储能示例参数：

- 额定容量；
- 最大充放电功率；
- 初始 SOC；
- 充放电效率；
- 接入电压等级。

双向 DC/DC 示例参数：

- 额定容量；
- 充电效率；
- 放电效率；
- 低压侧直流电压；
- 高压侧直流电压；
- 控制模式。

PCS 示例参数：

- 额定容量；
- 转换效率；
- 直流侧电压；
- 交流侧电压；
- 功率因数；
- 控制模式。

### 6.2 配置线路参数

双击线路，会弹出线路参数配置窗口。

线路参数包括：

- 线路长度；
- 单位电阻；
- 单位电抗；
- 最大载流量；
- 线路名称。

运行分析后，线路上会显示：

- 有功功率；
- 线路损耗；
- 负载率。

---

## 7. 母线设置

交流母线和直流母线支持调整长度和方向。

双击母线后，可配置：

- 额定电压等级；
- 布置方向；
- 母线长度；
- 电压上下限偏差；
- 实时电压。

布置方向参数：

```text
H：水平布置
V：竖直布置
```

修改后，母线会自动更新端口位置，并刷新相关连线。

---

## 8. 运行分析结果说明

点击“运行分析”后，右侧面板显示主要指标。

### 8.1 当前发电功率

统计光伏、风机、微燃机、燃料电池等电源当前出力。

### 8.2 当前负荷功率

统计电力负荷和电解槽等用电设备当前功率。

### 8.3 可再生能源占比

表示光伏和风机在 24 小时内的总发电量与日负荷电量的比例。

### 8.4 线路损耗

软件根据线路电阻、线路电流和功率流估算 I2R 损耗。

### 8.5 变换器损耗

软件根据变换器流经功率和转换效率估算损耗。

### 8.6 储能 SOC

软件根据储能容量、初始 SOC、充放电效率和最大功率估算 24 小时 SOC 变化。

### 8.7 LCOE

LCOE 为综合度电成本，用于衡量微电网单位供电成本。

---

## 9. 工程保存与加载

### 9.1 保存工程

点击“保存工程”，软件会将当前拓扑保存为 JSON 文件。

保存内容包括：

- 节点名称；
- 元件类型；
- 节点坐标；
- 节点参数；
- 线路两端节点；
- 线路端口编号；
- 线路参数；
- 线路走线方式。

### 9.2 加载工程

点击“加载工程”，选择之前保存的 JSON 文件，即可恢复拓扑和参数。

---

## 10. 结果导出

点击“导出结果”，可以将 24 小时运行结果导出为 CSV 文件。

导出字段包括：

- 时间；
- 负荷功率；
- 光伏功率；
- 风电功率；
- 可控电源功率；
- 储能功率；
- 储能 SOC；
- 线路损耗；
- 变换器损耗；
- 电网购电功率；
- 电网售电功率；
- 运行成本。

---

## 11. 常见问题

### 11.1 为什么光伏不能直接接交流母线？

光伏组件输出为直流电，需要通过 DC/DC 实现 MPPT 和电压匹配，再通过 DC/AC 逆变后接入交流母线。

### 11.2 为什么风机需要 AC/DC 和 DC/AC？

风机输出受风速影响，通常是频率和电压变化的交流。通过 AC/DC 整流到直流母线，再由 DC/AC 输出工频交流，是常见的全功率变流接入方式。

### 11.3 电池储能到底是直流还是交流？

电池本体是直流储能。若接入直流母线，通常通过双向 DC/DC；若接入交流母线，通常通过 PCS。软件中已经将二者拆分为独立元件。

### 11.4 为什么示例中有两个 DC/DC？

普通 DC/DC 主要用于光伏、燃料电池等直流电源的电压匹配；双向 DC/DC 专门用于储能充放电管理。

### 11.5 当前计算结果可以用于工程设计定值吗？

不建议直接用于工程定值。当前版本适合方案级分析、教学展示和论文结构说明。若需要工程精度，应引入更完整的 AC/DC 混合潮流、优化调度和设备约束模型。

